// import { FastifyPluginCallback } from "fastify";
// import { registerPhotoRoutes } from './photoRoutes';

// export const registerRoutes: FastifyPluginCallback = (app, opts, done) => {
//   app.register(registerPhotoRoutes, { prefix: '/photos' });
//   done();
// }